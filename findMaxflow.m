% Given line capacity, find the maximum flow.%% INPUTS: line capacity(adj)% OUTPUTS: a maximum line flow(adj) and the value(num) of the flow%% GB: last updated, Jan 20, 2019function [valf, adjMf] = findMaxflow(adjcf)    % The size of the capacity matrix.  [x_2,y_2] = size(adjcf);    % Initialize the flow.  adjf = zeros(x_2,y_2);  maxReach = 0;    % Keep augmenting the flow until no augmenting paths can be found  while maxReach == 0    [maxReach, adjf] = AugmentShortestPath(adjf,adjcf);  end  adjMf = adjf;  valf = sum(adjf(1,:));    